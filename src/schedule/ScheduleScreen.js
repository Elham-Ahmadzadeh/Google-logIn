import React from "react";
import { StyleSheet, View, Text } from "react-native";

const Schedule = () => {
  return (
    <View>
      <Text style={style.myText}>Schema</Text>
    </View>
  );
};
const style = StyleSheet.create({
  myText: {
    fontSize: 40,
  },
});
export default Schedule;
